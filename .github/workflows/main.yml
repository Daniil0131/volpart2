name: Deploy Vite to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # Устанавливаем зависимости (без package-lock можно)
      - run: npm install

      - run: npm run build

      # 🔎 Диагностика: покажем дерево dist и проверим index.html
      - name: Show dist tree
        run: |
          echo "----- dist tree -----"
          ls -la dist || true
          echo "---------------------"
          test -f dist/index.html || (echo "NO dist/index.html -> 404 неизбежен" && exit 1)

      # Публикуем именно dist
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
